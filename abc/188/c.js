function Main(input) {
  const N = +input.split('\n')[0]
  const temp = input.split('\n')[1]
  const [...a] = temp.split(' ').map(str => +str)

  // for (let i = 0; i < N ) {
  //   for (let j = o; j < N)
  // }


}
Main(require("fs").readFileSync("/dev/stdin", "utf8"));